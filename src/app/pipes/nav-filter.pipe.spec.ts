import { NavFilterPipe } from './nav-filter.pipe';

describe('NavFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NavFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
